[mapillary]
# Put your personal access token here (or leave blank to be prompted each run)
access_token = 

# Exactly one of the following; leave others blank
sequence_key = y0b917m09znglhifax9sqv
image_key    =
bbox         = 

# Limit applies only to bbox queries
limit = 1

# Root output folder (will be created if missing)
output_root = downloads
